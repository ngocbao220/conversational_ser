run_name: unfreeze4_ses5_wavlm_cdm
experiment_name: unfreeze4_ses5_wavlm_cdm
output_dir: results/unfreeze4_ses5/wavlm/cdm
seed: 42
dataset:
  name: iemocap
  iemocap_root: data/iemocap
  auto_download: true
  kaggle_dataset: sangayb/iemocap
  test_session: 5
  validation_ratio: 0.1
  sampling_rate: 16000
  max_duration_seconds: null
model:
  wavlm_model_name: microsoft/wavlm-base
  num_labels: 4
  freeze_wavlm: true
  unfreeze_last_n_layers: 4
  memory_dim: 128
  temporal_feature_dim: 16
  use_temporal_features: false
  temporal_feature_mode: zero
  dropout: 0.2
precompute:
  enabled: false
  pooling: mean
  cache_path: results/wavlm_shared/cache/wavlm_mean_embeddings.pt
  force_recompute: false
  batch_size: 16
cross_session:
  enabled: false
  test_sessions:
  - 5
  run_name: null
training:
  device: auto
  batch_size: 16
  eval_batch_size: 16
  max_epochs: 100
  wavlm_batch_size: 8
  eval_wavlm_batch_size: 8
  learning_rate: 5.0e-05
  learning_rate_classifier: 0.0001
  weight_decay: 0.0001
  scheduler: cosine
  warmup_ratio: 0.1
  gradient_clip: 0.5
  use_class_weights: false
  num_workers: 0
  progress_bar: true
wandb:
  use_wandb: true
  project: conversational-SER
  run_name: unfreeze4_ses5_wavlm_cdm
  entity: null
  mode: online
